source "https://rubygems.org"

# Local dev dependencies for GitHub Pages-compatible Jekyll site.
# Keep this light so the site can be previewed without relying on system-wide gems.

gem "jekyll", "~> 4.2"
gem "webrick", "~> 1.8"

# Optional but common. (Keep this repo lightweight; add more plugins as needed.)
